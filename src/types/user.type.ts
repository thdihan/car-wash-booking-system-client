export type TUser = {
    id: string;
    email: string;
    role: string;
    iat: string;
    exp: string;
};

export type TCustomer = {
    _id: string;
    name: string;
    email: string;
    phone: string;
    address: string;
};
